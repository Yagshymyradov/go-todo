version: "3.9"
services:
  db:
    image: postgres:16
    ports:
      - "5432:5432"
    environments:
      POSTGRES_USER: todo
      POSTGRES_PASSWORD: todo
      POSTGRES_DB: todo
    volumes:
      - db_data:/var/lib/postgresql/data
volumes:
  db_data: